<% layout("/layouts/boilerplate")%>

<div class="coming-soon-wrapper">
  <div class="coming-soon-content">
    <div class="icon-container">
      <i class="fa-solid fa-universal-access"></i>
    </div>
    <h1>Accessibility</h1>
    <p class="subtitle">Travel for everyone, everywhere</p>
    
    <div class="coming-soon-message">
      <h2>Coming Soon!</h2>
      <p>We're building accessibility features and partnering with hosts to ensure comfortable accommodations for travelers with different needs and abilities.</p>
      
      <div class="cta-buttons">
        <a href="/contact" class="btn-primary">Request Features</a>
        <a href="/listings" class="btn-secondary">Browse Accessible Listings</a>
      </div>
    </div>
  </div>
</div>

<style>
  body { background: #f7faf9; }
  .coming-soon-wrapper {
    max-width: 680px;
    margin: 0 auto;
    padding: 3.5rem 1.5rem;
    text-align: center;
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .coming-soon-content {
    background: #ffffff;
    padding: 2.5rem;
    border-radius: 14px;
    box-shadow: 0 10px 30px rgba(7,10,21,0.06);
    width: 100%;
  }

  .icon-container {
    width: 88px;
    height: 88px;
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.25rem;
    font-size: 2rem;
  }

  .coming-soon-content h1 { color: #064e3b; font-size: 2.25rem; margin-bottom: .4rem; }
  .subtitle { color: #475569; font-size: 1rem; margin-bottom: 1rem; }

  .coming-soon-message h2 { color: #0f172a; margin-bottom: .75rem; font-size: 1.25rem; }
  .coming-soon-message p { color: #64748b; line-height: 1.6; margin-bottom: 1rem; }

  .cta-buttons { display:flex; gap: .75rem; justify-content:center; margin-top: 1rem; }

  .btn-primary { background: #059669; color: white; padding: .7rem 1.25rem; border-radius: 8px; text-decoration:none; font-weight:700; }
  .btn-secondary { background: transparent; color: #065f46; border: 2px solid #e6f7ef; padding: .65rem 1.2rem; border-radius:8px; text-decoration:none; font-weight:600; }

  @media (max-width: 768px) { .coming-soon-content h1 { font-size: 1.6rem; } .cta-buttons { flex-direction: column; } }
</style>