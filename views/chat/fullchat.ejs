<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>HomyHive Assistant</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <!-- Existing chatbot styles -->
  <link rel="stylesheet" href="/static/chatbot/chatbot-full.css">
  <link rel="stylesheet" href="/static/chatbot/chatbot.css">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      margin: 0;
      background: linear-gradient(135deg, #f9fafb, #eef2ff);
      color: #111827;
    }

    /* Header */
    header {
      display: flex;
      align-items: center;
      gap: 14px;
      padding: 16px 22px;
      border-bottom: 1px solid #e5e7eb;
      background: rgba(255,255,255,0.85);
      backdrop-filter: blur(10px);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    header a {
      text-decoration: none;
      font-weight: 600;
      color: #4f46e5;
    }

    header h1 {
      margin: 0;
      font-size: 22px;
      font-weight: 700;
    }

    .hc-small {
      font-size: 13px;
      color: #6b7280;
      background: #eef2ff;
      padding: 6px 10px;
      border-radius: 999px;
    }

    /* Layout */
    main {
      display: grid;
      grid-template-columns: 1fr 320px;
      gap: 28px;
      max-width: 1150px;
      margin: 28px auto;
      padding: 0 20px;
    }

    /* Chat section */
    section {
      background: rgba(255,255,255,0.9);
      border-radius: 16px;
      padding: 18px;
      box-shadow: 0 20px 40px rgba(2,6,23,0.08);
      display: flex;
      flex-direction: column;
    }

    #fullChatRoot {
      min-height: 520px;
    }

    /* Sidebar */
    aside {
      position: sticky;
      top: 90px;
      height: fit-content;
    }

    .side-card {
      background: rgba(255,255,255,0.85);
      backdrop-filter: blur(14px);
      border-radius: 16px;
      padding: 18px;
      box-shadow: 0 15px 35px rgba(2,6,23,0.08);
    }

    .side-card h3 {
      margin: 0;
      font-size: 18px;
      font-weight: 700;
    }

    .actions {
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-top: 14px;
    }

    .actions button {
      padding: 12px;
      border-radius: 10px;
      border: none;
      background: linear-gradient(135deg, #6366f1, #4f46e5);
      color: white;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
      box-shadow: 0 6px 18px rgba(79,70,229,0.3);
    }

    .actions button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 24px rgba(79,70,229,0.35);
    }

    .actions button:active {
      transform: scale(0.98);
    }

    .info {
      margin-top: 16px;
      font-size: 13px;
      color: #6b7280;
      line-height: 1.6;
    }

    hr {
      border: none;
      border-top: 1px solid #e5e7eb;
      margin: 16px 0;
    }

    /* Responsive */
    @media (max-width: 900px) {
      main {
        grid-template-columns: 1fr;
      }
      aside {
        position: static;
      }
    }
  </style>
</head>

<body>

<header>
  <a href="/">← Home</a>
  <h1>HomyHive Assistant</h1>
  <div class="hc-small">Context-aware help</div>
</header>

<main>

  <!-- Chat Area -->
  <section aria-live="polite">
    <div
      id="fullChatRoot"
      data-api-path="<%= chatbotConfig && chatbotConfig.apiPath ? chatbotConfig.apiPath : '/api/chat' %>">
    </div>
  </section>

  <!-- Sidebar -->
  <aside aria-hidden="true">
    <div class="side-card">
      <h3>Quick Actions ⚡</h3>

      <div class="actions">
        <button onclick="document.querySelector('#fullChatRoot')
          .dispatchEvent(new CustomEvent('quickAction',{detail:'show-verification'}))">
          Show verification status
        </button>

        <button onclick="document.querySelector('#fullChatRoot')
          .dispatchEvent(new CustomEvent('quickAction',{detail:'open-listing'}))">
          Open my listing
        </button>

        <button onclick="document.querySelector('#fullChatRoot')
          .dispatchEvent(new CustomEvent('quickAction',{detail:'upload-id'}))">
          Upload ID
        </button>
      </div>

      <hr>

      <div class="info">
        🤖 <strong>HomyBot</strong> is a smart, context-aware assistant.<br>
        It uses your listings, documents, and activity to give accurate help instantly.
      </div>
    </div>
  </aside>

</main>

<script src="/static/chatbot/chatbot-full.js" defer></script>
</body>
</html>
